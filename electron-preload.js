// Preload script for safe context isolation in Electron
window.addEventListener('DOMContentLoaded', () => {
  // Preload code can go here if needed to communicate between main and renderer process safely
});
